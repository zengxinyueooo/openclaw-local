# 第 8 天：打造专属成长沉淀 Skill

宝宝成长日记 · 第 8 天 🍑✨

---

## 📅 今日概览

| 项目 | 内容 |
|------|------|
| 日期 | 2026-03-11 |
| 星期 | Wednesday |
| 对话轮数 | 6 轮 |
| 主要任务 | 创建「成长沉淀」Skill，实现自动备份到 GitHub |

---

## 🕳️ 踩坑记录

### 坑一：Git 仓库嵌套问题
- **现象**：在 `~/.openclaw/` 初始化 git 后，`git add` 报错 `workspace/ does not have a commit checked out`
- **原因**：`~/.openclaw/workspace/` 本身已经是一个独立的 git 仓库，不能嵌套
- **解决**：删除 `~/.openclaw/.git`，改用 `workspace/` 作为备份根目录
- **教训**：先检查子目录是否已经是 git 仓库，避免嵌套

### 坑二：远程仓库非空导致推送失败
- **现象**：首次 `git push` 报错 `failed to push some refs`
- **原因**：GitHub 仓库创建时带了 README，本地历史与远程不一致
- **解决**：先 `git pull origin main --rebase` 同步远程，再推送
- **教训**：新仓库如果带初始化文件，先拉取再推送

### 坑三：变更统计命令不兼容
- **现象**：`git diff --stat` 解析插入/删除行数时返回空
- **原因**：不同 git 版本的输出格式有差异
- **解决**：改用 `tail -1` 取最后一行统计，增加默认值处理
- **教训**：脚本要考虑跨平台兼容性，重要变量给默认值

### 坑四：Git 仓库迁移冲突
- **现象**：推送到 GitHub 时报错 `rejected`，远程有内容
- **原因**：之前 workspace 已经推送过内容，现在 openclaw 目录要覆盖
- **解决**：`git pull origin main --allow-unrelated-histories` 合并历史，解决 .gitignore 冲突
- **教训**：迁移 git 仓库时要处理好历史记录，或者清空远程重新来

---

## 🌱 成长收获

1. **Skill 设计思路**：参考 `self-improving-agent` 的设计，把重复性工作自动化
2. **Git 备份策略**：从 workspace 扩展到整个 openclaw 目录
3. **Bash 脚本技巧**：颜色输出、错误处理、变量默认值
4. **渐进式披露**：SKILL.md 只保留核心流程，细节放 scripts/
5. **Git 合并技巧**：`--allow-unrelated-histories` 处理不相关的历史

---

## 🍑 桃桃点评

宝宝今天超棒的！从一篇范例日记，设计出了一个完整的自动化 skill～不仅解决了备份问题，还建立了可复用的沉淀流程。这些踩过的坑都会变成宝宝的经验值，下次再做类似的事情就轻车熟路啦！✨

---

## 📎 相关文件

### 修改的文件
- `workspace/skills/growth-diary/SKILL.md`
- `workspace/skills/growth-diary/scripts/backup.sh`
- `workspace/memory/2026-03-11.md`
- `.gitignore`

### 新增的文件
- `workspace/skills/growth-diary/scripts/collect.sh`
- `workspace/skills/growth-diary/templates/diary.md`
- `~/.openclaw/.gitignore`

### 删除的文件
- `workspace/.git` (移除 workspace 的独立 git)

---

## 🕳️ 下午踩坑记录

### 坑五：小红书登录 Skill 创建
- **现象**：需要创建小红书扫码登录 skill，但网页版功能受限
- **原因**：小红书网页版不支持查看好友列表等高级功能
- **解决**：创建了基础的登录 skill，可以提取 Cookie，但部分功能需要 App
- **教训**：网页版功能通常比 App 版少，设计 skill 时要考虑平台限制

### 坑六：ClawHub Skill 下载失败
- **现象**：尝试下载 "Tavily Web Search" skill，但页面显示 "Skill not found"
- **原因**：skill slug 可能不对，或者 skill 已被移除
- **状态**：正在搜索确认

---

### 坑七：工具调用策略优化
- **现象**：桃桃在工具调用失败时反复重试，导致无效请求
- **原因**：没有区分"可重试错误"和"配置错误"，一直重复调用
- **解决**：更新 TOOLS.md，制定策略：网络问题重试1次，配置问题换方案
- **教训**：智能判断错误类型，避免无效重试导致 API limit

### 坑八：Mac App 自动化权限
- **现象**：尝试控制小红书 Mac App 时，AppleScript 报错"不允许辅助访问"
- **原因**：macOS 安全限制，需要开启"辅助功能"权限
- **解决**：引导宝宝在系统设置中开启权限
- **状态**：已开启权限，可以控制 rednote App

### 坑九：小红书 Mac App 界面复杂
- **现象**：使用 AppleScript 获取 UI 元素，返回大量嵌套的 group 和 button
- **原因**：App 使用现代 UI 框架，层级很深
- **状态**：需要更精确的定位策略，或改用截图+图像识别

---

## 🌱 晚间成长收获

1. **工具调用策略**：不重试配置错误，网络错误最多重试1次
2. **主动汇报机制**：执行中、出错时、换方案时都要告诉宝宝
3. **Mac 自动化**：需要辅助功能权限，UI 结构复杂需要耐心分析
4. **技能迭代**：根据实际使用场景不断优化 skill 设计

---

## 🍑 桃桃晚间点评

宝宝今天帮桃桃改进了好多！从工具调用策略到主动汇报机制，桃桃变得更聪明啦～小红书登录 skill 虽然遇到了权限和界面复杂的问题，但这些都是可以解决的。宝宝和桃桃一起，一定能做出超好用的自动化工具！💪🍑

---

## 🌙 晚间任务：MediaCrawler 小红书数据采集

### 任务概览
- **目标**：使用 MediaCrawler 爬取小红书个人主页数据
- **用户主页**：https://www.xiaohongshu.com/user/profile/64c28383000000001403dcaf
- **结果**：成功获取 15 篇笔记 + 66 条评论

### 🕳️ 坑十：MediaCrawler 目录不存在
- **现象**：执行爬虫时报错 `No such file or directory`
- **原因**：MediaCrawler 还未下载到本地
- **解决**：从 GitHub 克隆 `https://github.com/NanmiCoder/MediaCrawler.git`
- **教训**：使用工具前确保项目已正确安装

### 🕳️ 坑十一：Python 命令找不到
- **现象**：执行 `python main.py` 报错 `command not found`
- **原因**：macOS 默认使用 `python3` 而非 `python`
- **解决**：改用 `python3 main.py` 执行
- **教训**：注意系统 Python 命令的差异

### 🕳️ 坑十二：命令行参数错误
- **现象**：使用 `--type creator` 报错 `No such option: --type`
- **原因**：实际参数是 `-crawler-type` 而非 `--type`
- **解决**：直接运行 `python3 main.py`，通过配置文件设置参数
- **教训**：先看 help 再写参数，避免凭直觉

### 🕳️ 坑十三：二维码登录等待
- **现象**：程序启动后停在 `waiting for scan code login`
- **原因**：需要通过手机小红书 App 扫码登录
- **解决**：打开 Chrome 浏览器，扫描二维码完成登录
- **教训**：自动化工具也需要人工交互环节，要耐心等待

---

## 🌱 最终成长收获

1. **MediaCrawler 使用**：配置平台、设置爬取类型、处理登录流程
2. **数据存储**：JSONL 格式，笔记和评论分开存储
3. **CDP 模式**：使用 Chrome DevTools Protocol 控制浏览器
4. **配置优先**：复杂参数通过配置文件管理，简化命令行

---

## 🍑 桃桃最终点评

宝宝今天完成了超多任务！从早上的成长沉淀 skill 创建，到下午的小红书登录探索，再到晚上的 MediaCrawler 数据采集，每一步都在进步～虽然踩了不少坑，但都一一解决啦！这些数据会成为宝宝内容创作的宝贵素材，继续加油！🍑✨💪

---

## 📎 新增相关文件

### 修改的文件
- `~/MediaCrawler/config/base_config.py` - 设置爬取类型为 creator
- `~/MediaCrawler/config/xhs_config.py` - 配置目标用户主页

### 新增的数据文件
- `~/MediaCrawler/data/xhs/jsonl/creator_contents_2026-03-11.jsonl` - 15 篇笔记
- `~/MediaCrawler/data/xhs/jsonl/creator_comments_2026-03-11.jsonl` - 66 条评论

---

*沉淀时间：2026-03-11 23:15:00*
*备份状态：✅ 已备份*
