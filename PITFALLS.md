# PITFALLS.md

踩坑点记录 🍑

> 记录那些需要牢记、避免再犯的问题

---

## 配置变更规范

**任何配置变更前，必须先读官方文档。确认参数含义、依赖、边界条件。改完观察至少一轮对话。**

## 子 Agent 任务完成必须立即返回

**子 Agent 完成任务后，无论当前是否在聊天，必须立即中断当前对话返回结果，不能等宝宝来问。**
- 错误：子 Agent 完成，小桃继续聊天，没有及时返回
- 正确：收到完成通知 → 立即返回结果 → 再继续其他对话

## 异步操作必须确认成功后再汇报

**执行 git push 等异步操作时，必须等待确认成功后再向宝宝汇报，不能提前说"完成"。**
- 错误：push 还在运行中就告诉宝宝"提交成功"
- 正确：等待命令执行完毕 → 检查状态确认成功 → 再汇报结果

## 修改文档前必须先展示给用户确认

**更新 .md 等重要文件前，必须先展示变更内容，等用户说"可以"后再执行。**
- 错误：直接编辑文件，事后才告知用户
- 正确：展示拟修改内容 → 等待用户确认 → 执行修改
- 违反规则：AGENTS.md 中「操作前说明做什么+为什么，宝宝同意后执行」
- 触发：用户纠正小桃

---

*持续更新中...*
